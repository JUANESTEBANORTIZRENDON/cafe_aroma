services:
  cafe-aroma:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Montar el código para desarrollo (opcional)
      - .:/app
    restart: unless-stopped
    container_name: cafe-aroma-app
